[project]
name = "clashbot-engine"
version = "0.1.0"
description = "Deterministic Clash Royale-style simulator prototype with a debug renderer."
requires-python = ">=3.8"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
